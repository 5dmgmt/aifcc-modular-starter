# Phase 5 未解消リスト(aifcc-modular-starter)

実行日: 2026-07-02 / 実行: Sonnet(REFACTOR_PLAN.md Phase 5)

## npm audit 結果

- 実行前: low1 / moderate6 / high1 / critical0(計8。next middleware bypass 系CVE複数・uuid・postcss含む)
- `npm audit fix`(非force)実行 → next が `^16.1.7` 範囲内で `16.2.10` に自動解決、middleware bypass系CVE・uuid脆弱性を全て解消
- 実行後: moderate1(postcss) のみ残存

## 未解消(major更新が必要なため見送り)

- **postcss**(moderate): `next` パッケージ内部依存。修正には next を `9.3.3` へダウングレードする必要があり、ランブックの `--force` 禁止方針により見送り

## バージョン整合

- next: `^16.1.7`(package.json変更なし)→ 解決版 `16.2.10`(目標16.2.x系と一致)
- react / react-dom: 既に `^19.2.4`(目標と一致、変更不要)

## 検証結果

- tsc緑・eslint 0件・build成功
